name: alwazirchat
description: Al-Wazir Chat - الفهد
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # قاعدة البيانات المحلية
  sqflite: ^2.4.2
  path: ^1.9.1
  nearby_connections: ^4.3.0

  # المراسلة بين الأجهزة القريبة بدون إنترنت
  nearby_connections: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
