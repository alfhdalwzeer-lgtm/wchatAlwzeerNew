name: al_wazir_chat
description: تطبيق مراسلة متكامل للشات والطاقة الشمسية والاتصال.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1 # لإدارة الحالة وتغيير الثيمات والشاشات

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # أضف مسارات الصور أو الأيقونات هنا إذا احتجت لاحقاً
  # assets:
  #   - assets/images/
